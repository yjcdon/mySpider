/:用它只能按标签的层级顺序一层层来写(/在开头则从根节点开始定位)(一个/表示一个层级)
//:它在开头可以从任意标签开始,在中间表示多个层级
想定位某个属性,标签名[@属性名="属性值"]
想定位某个属性的某个标签,标签名[@属性名="属性值"]/标签名;若有同名的标签,则标签名[@属性名="属性值"]/标签名[第几个就填几的数字]
                                                                                           ↑从1开始数,不是0
@:定位所在标签的属性名


获取文本:/text()
获取属性值:定位好标签后,/@属性名