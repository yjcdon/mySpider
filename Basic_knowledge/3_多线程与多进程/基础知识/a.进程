多进程process:操作系统同时支持多个处理器的能力.
    一个应用程序会被分解成多个独立运行的较小的程序,操作系统会将这些线程分配到多核处理器，以提升系统性能。
    可以同时使用多个CPU进行并行运算

缺点:占用资源多

人话:我们打开360,它就是一个进程
    假设我们的计算机只有一个单核的处理器，然后同时被分配了几个任务，那么它就不得不在各个任务中来回切换，
    短暂地执行其中一个任务，然后中断，然后短暂地执行下一个任务，以保持所有的进程都在运行。
    所以同时需要完成的任务越多，同时跟踪所有的任务就越困难。这就是多进程的必要性，也是多核处理器的威力所在。

    你看任务管理器,那里很多的程序,每个程序都有一个独立的进程

    单进程:这就像一个厨师在做面条，切几秒钟菜，跑去揉几下面，再赶紧去看煮的汤。
    多进程:现在厨房里有专门的揉面师傅，备菜师傅，煮汤师傅。

使用场景:CPU密集型计算

